/**
 * Vitest global setup file (Phase 8).
 *
 * Wired via vitest.config.ts test.setupFiles. Currently a stub — Plans 03/05
 * may extend with global mocks (e.g. matchMedia) if a component test needs them.
 *
 * Intentionally NOT importing @testing-library/jest-dom — the codebase asserts
 * via direct DOM property reads, not custom matchers.
 */

export {};
