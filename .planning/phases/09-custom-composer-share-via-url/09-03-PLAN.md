---
phase: 09-custom-composer-share-via-url
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Toast.tsx
  - src/components/CustomCard.tsx
  - src/components/__tests__/Toast.test.tsx
  - src/components/__tests__/CustomCard.test.tsx
autonomous: true
requirements: [COMP-01]
tags: [toast, custom-card, atomic-component, parallel-file]

must_haves:
  truths:
    - "Toast renders nothing when message is null (hidden state)"
    - "Toast renders a role='status' aria-live='polite' div when message is non-null"
    - "Toast auto-dismisses by calling onDismiss after durationMs (via setTimeout in useEffect)"
    - "CustomCard renders as a button with dashed border, '+' glyph, label 'Custom', and description 'Compose your own alarm' (D-09 LOCKED)"
    - "CustomCard's aria-label is 'Open custom alarm composer' for SR clarity (UI-SPEC L257)"
    - "CustomCard onClick fires when the button is clicked"
    - "CustomCard's class string matches the verbatim UI-SPEC L194-217 string (dashed border, bg-bg, hover ring)"
    - "PresetCard.tsx is NOT modified (parallel-file pattern per CONTEXT.md `canonical_refs` strong preference for parallel CustomCard.tsx (see 09-CONTEXT.md `<canonical_refs>` section) + RESEARCH Pattern 8)"
  artifacts:
    - path: "src/components/Toast.tsx"
      provides: "Shared live-region toast for share-success + decode-error use cases"
      exports: ["default Toast"]
      min_lines: 25
    - path: "src/components/CustomCard.tsx"
      provides: "4th Dashboard card (parallel to PresetCard; dashed-border 'Custom' card)"
      exports: ["default CustomCard"]
      min_lines: 20
    - path: "src/components/__tests__/Toast.test.tsx"
      provides: "Toast visibility, ARIA role, auto-dismiss timing"
      min_lines: 30
    - path: "src/components/__tests__/CustomCard.test.tsx"
      provides: "CustomCard visible labels, aria-label, onClick wiring"
      min_lines: 25
  key_links:
    - from: "src/components/CustomCard.tsx"
      to: "src/components/PresetCard.tsx"
      via: "READ-ONLY analog — class string idioms mirrored, NOT imported"
      pattern: "parallel-file"
    - from: "src/components/Toast.tsx"
      to: "react"
      via: "useEffect for setTimeout auto-dismiss"
      pattern: "useEffect"
---

<objective>
Ship two small, atomic, parallel-safe leaf components: `Toast.tsx` (shared live-region for share-confirmation + decode-error per RESEARCH Pattern 11) and `CustomCard.tsx` (the parallel-to-PresetCard dashed-border 4th dashboard card per RESEARCH Pattern 8). Locks the visual half of COMP-01 (Custom entry button).

Purpose: Get both atomic components onto the Wave-1 fast track because they have zero dependencies on other Phase 9 work — Toast only needs React's useEffect; CustomCard only needs the existing Tailwind palette. Both are consumed in Wave 4 (Composer.tsx for Toast wiring) and Wave 5 (Dashboard.tsx for CustomCard mount).

Output: Two new components + their tests. PresetCard.tsx remains BYTE-IDENTICAL (per CONTEXT.md `canonical_refs` strong preference for parallel CustomCard.tsx — see 09-CONTEXT.md `<canonical_refs>` section).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-custom-composer-share-via-url/09-CONTEXT.md
@.planning/phases/09-custom-composer-share-via-url/09-RESEARCH.md
@.planning/phases/09-custom-composer-share-via-url/09-PATTERNS.md
@.planning/phases/09-custom-composer-share-via-url/09-UI-SPEC.md
@src/components/PresetCard.tsx

<interfaces>
<!-- From src/components/PresetCard.tsx:10-14 (READ-ONLY analog — CustomCard mirrors this API style) -->
```typescript
interface PresetCardProps {
  name: string;
  description: string;
  onStart: () => void;
}
```

<!-- CustomCard prop signature (from 09-PATTERNS.md:65 + UI-SPEC L196) -->
```typescript
interface CustomCardProps {
  onClick: () => void;  // opens composer modal (semantically distinct from onStart)
}
```

<!-- Toast prop signature (from 09-RESEARCH.md:1104-1108 + UI-SPEC L488) -->
```typescript
interface ToastProps {
  message: string | null;  // null = hidden
  durationMs: number;      // auto-dismiss delay
  onDismiss: () => void;   // called by setTimeout when durationMs elapses
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/components/CustomCard.tsx (parallel to PresetCard with dashed border)</name>
  <files>src/components/CustomCard.tsx, src/components/__tests__/CustomCard.test.tsx</files>
  <read_first>
    - src/components/PresetCard.tsx (READ-ONLY analog — copy class-string idioms; NEVER modify per CONTEXT.md `canonical_refs` strong preference for parallel CustomCard.tsx (see 09-CONTEXT.md `<canonical_refs>` section))
    - .planning/phases/09-custom-composer-share-via-url/09-UI-SPEC.md (lines 194-217 — verbatim Tailwind class string for CustomCard.tsx)
    - .planning/phases/09-custom-composer-share-via-url/09-PATTERNS.md (lines 43-95 — Pattern Assignments for CustomCard.tsx with verbatim copy)
    - .planning/phases/09-custom-composer-share-via-url/09-RESEARCH.md (lines 954-994 — Pattern 8 parallel-file rationale)
    - .planning/phases/09-custom-composer-share-via-url/09-CONTEXT.md (lines 62-70 — D-08/D-09 LOCKED visual treatment)
    - src/components/__tests__/Dashboard.test.tsx (READ-ONLY test idiom — copy describe/it + afterEach cleanup pattern)
  </read_first>
  <behavior>
    - CustomCard renders a button with type="button"
    - Visible text content: "Custom" (title) and "Compose your own alarm" (description) per D-09 LOCKED
    - aria-label="Open custom alarm composer" per UI-SPEC L257
    - aria-hidden="true" on the '+' glyph (UI-SPEC L210)
    - Click invokes the onClick prop exactly once
    - Class string matches UI-SPEC L199-207 verbatim: w-full p-6 rounded-2xl border-2 border-dashed border-border bg-bg text-left min-h-[80px] active:scale-[0.98] transition-transform duration-150 hover:border-text-secondary/60 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-sage/40
    - Inner layout uses flex items-start gap-3 with the '+' span and a flex-1 div for the text column
    - '+' span class: text-2xl text-sage font-light leading-none mt-0.5
    - Title <p>: text-xl font-semibold text-text-primary
    - Description <p>: text-sm text-text-secondary mt-1
  </behavior>
  <action>
    Create `src/components/CustomCard.tsx` verbatim from 09-UI-SPEC.md L194-217:

    ```tsx
    /**
     * CustomCard — the 4th Dashboard card (D-08 LOCKED, after the 4 x 4 card).
     *
     * Parallel-file pattern per CONTEXT.md `canonical_refs` strong preference for parallel
     * CustomCard.tsx (see 09-CONTEXT.md `<canonical_refs>` section) and RESEARCH Pattern 8
     * (09-RESEARCH.md:954-994): PresetCard.tsx is treated as frozen (CONTEXT.md
     * `canonical_refs` "strong preference"); cloning the shell into a sibling file is the
     * cleaner path than extending PresetCard with a variant prop. Visual deltas (D-09 LOCKED):
     *   - Border: border-2 border-dashed (vs PresetCard's border)
     *   - Background: bg-bg (vs PresetCard's bg-white/60) — creative space, not preset surface
     *   - '+' glyph in the top-left of an inner flex row, sage color
     *   - Hover deepens the border tone instead of scaling
     *   - Explicit focus-visible ring (dashed border weakens the default browser ring)
     *
     * Prop signature differs intentionally: onClick (opens composer modal) instead of
     * onStart (PresetCard's "launch an alarm" semantic). Labels are baked in — there's
     * only one CustomCard in the app.
     */

    interface CustomCardProps {
      onClick: () => void;
    }

    export default function CustomCard({ onClick }: CustomCardProps) {
      return (
        <button
          onClick={onClick}
          type="button"
          aria-label="Open custom alarm composer"
          className="w-full p-6 rounded-2xl border-2 border-dashed border-border bg-bg text-left min-h-[80px] active:scale-[0.98] transition-transform duration-150 hover:border-text-secondary/60 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-sage/40"
        >
          <div className="flex items-start gap-3">
            <span aria-hidden="true" className="text-2xl text-sage font-light leading-none mt-0.5">+</span>
            <div className="flex-1">
              <p className="text-xl font-semibold text-text-primary">Custom</p>
              <p className="text-sm text-text-secondary mt-1">Compose your own alarm</p>
            </div>
          </div>
        </button>
      );
    }
    ```

    Create `src/components/__tests__/CustomCard.test.tsx` following Dashboard.test.tsx idiom:

    ```tsx
    import { describe, it, expect, vi, afterEach } from 'vitest';
    import { render, screen, fireEvent, cleanup } from '@testing-library/react';
    import CustomCard from '../CustomCard';

    afterEach(() => {
      cleanup();
    });

    describe('CustomCard', () => {
      it('renders the locked label "Custom" and description "Compose your own alarm" per D-09', () => {
        render(<CustomCard onClick={vi.fn()} />);
        expect(screen.getByText('Custom')).toBeTruthy();
        expect(screen.getByText('Compose your own alarm')).toBeTruthy();
      });

      it('exposes aria-label "Open custom alarm composer" for screen readers (UI-SPEC L257)', () => {
        render(<CustomCard onClick={vi.fn()} />);
        const button = screen.getByLabelText('Open custom alarm composer');
        expect(button.tagName).toBe('BUTTON');
      });

      it('renders the dashed-border class string per UI-SPEC L199-207', () => {
        render(<CustomCard onClick={vi.fn()} />);
        const button = screen.getByLabelText('Open custom alarm composer');
        expect(button.className).toContain('border-2');
        expect(button.className).toContain('border-dashed');
        expect(button.className).toContain('bg-bg');
      });

      it('invokes onClick when clicked', () => {
        const handleClick = vi.fn();
        render(<CustomCard onClick={handleClick} />);
        fireEvent.click(screen.getByLabelText('Open custom alarm composer'));
        expect(handleClick).toHaveBeenCalledTimes(1);
      });

      it('marks the "+" glyph as aria-hidden (decorative)', () => {
        render(<CustomCard onClick={vi.fn()} />);
        const plus = screen.getByText('+');
        expect(plus.getAttribute('aria-hidden')).toBe('true');
      });
    });
    ```

    **CRITICAL: Do NOT modify src/components/PresetCard.tsx.** Per CONTEXT.md `canonical_refs` strong preference for parallel CustomCard.tsx (see 09-CONTEXT.md `<canonical_refs>` section) + RESEARCH Pattern 8 + 09-PATTERNS.md:94, PresetCard is treated as frozen. Parallel file only.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/CustomCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f src/components/CustomCard.tsx && test -f src/components/__tests__/CustomCard.test.tsx`
    - Locked labels present: `grep -E "Custom|Compose your own alarm" src/components/CustomCard.tsx` returns ≥ 2 matches
    - aria-label present: `grep 'aria-label="Open custom alarm composer"' src/components/CustomCard.tsx` returns 1 match
    - Dashed border class present: `grep "border-2 border-dashed border-border" src/components/CustomCard.tsx` returns 1 match
    - Min-height present: `grep "min-h-\[80px\]" src/components/CustomCard.tsx` returns 1 match
    - Focus ring present: `grep "focus-visible:ring-2 focus-visible:ring-sage/40" src/components/CustomCard.tsx` returns 1 match
    - '+' glyph with aria-hidden: `grep -A1 'aria-hidden="true"' src/components/CustomCard.tsx | grep -E "\+" ` returns ≥ 1 match
    - PresetCard.tsx UNCHANGED: `git diff --quiet src/components/PresetCard.tsx` exits 0
    - All tests pass: `npx vitest run src/components/__tests__/CustomCard.test.tsx` exits 0
    - tsc clean: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    CustomCard renders as a button with the verbatim D-09 visual treatment (dashed border, '+' in sage, "Custom" / "Compose your own alarm" labels). PresetCard.tsx untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/components/Toast.tsx (role=status aria-live=polite with auto-dismiss)</name>
  <files>src/components/Toast.tsx, src/components/__tests__/Toast.test.tsx</files>
  <read_first>
    - .planning/phases/09-custom-composer-share-via-url/09-RESEARCH.md (lines 1088-1128 — Pattern 11 full implementation)
    - .planning/phases/09-custom-composer-share-via-url/09-UI-SPEC.md (lines 484-498 — verbatim Tailwind class string)
    - .planning/phases/09-custom-composer-share-via-url/09-UI-SPEC.md (lines 500-510 — auto-dismiss timings + role rationale)
    - .planning/phases/09-custom-composer-share-via-url/09-PATTERNS.md (lines 308-358 — Toast pattern assignment)
    - src/components/__tests__/Dashboard.test.tsx (READ-ONLY test idiom)
  </read_first>
  <behavior>
    - When `message === null`, Toast renders nothing (`return null`)
    - When `message` is a non-null string, Toast renders a <div role="status" aria-live="polite" aria-atomic="true"> containing the message text
    - The visible div has class: fixed left-1/2 -translate-x-1/2 bottom-8 z-50 px-4 py-3 rounded-xl bg-text-primary/95 text-bg text-sm shadow-lg max-w-[calc(100vw-2rem)] text-center transition-opacity duration-200 (plus opacity-100 when visible)
    - After `durationMs` milliseconds, onDismiss is called exactly once (via setTimeout in useEffect)
    - When the message prop changes, the previous timeout is cleared and a new one starts
    - When the component unmounts, any pending setTimeout is cleared
    - WCAG 2.2.1 (Timing Adjustable) — the 3s/4s timings are below 5s, but qualify for the "non-essential informational" exception (RESEARCH 09-RESEARCH.md:1174)
  </behavior>
  <action>
    Create `src/components/Toast.tsx` per RESEARCH Pattern 11 (lines 1099-1128) + UI-SPEC class string (lines 484-498):

    ```tsx
    /**
     * Toast — shared live-region notification for share-success + decode-error (D-18, D-19).
     *
     * Single-instance pattern (no queue): the latest message replaces any prior one.
     * Realistic emit cases (share-success, decode-error, share-fail) all fire from
     * explicit user actions and rarely overlap — the latest is the most relevant.
     *
     * ARIA: role="status" + aria-live="polite" announces the message after the
     * screen reader's current utterance finishes — NOT assertive, which would
     * interrupt mid-sentence (incompatible with the zen calm aesthetic).
     *
     * WCAG 2.2.1 (Timing Adjustable): the locked 3s share-success and 4s share-rejection /
     * decode-failure timings (D-18, D-19) are below the 5s floor but qualify for the
     * "non-essential informational" exception — no action is required to proceed.
     * See 09-RESEARCH.md:1174.
     */

    import { useEffect } from 'react';

    interface ToastProps {
      message: string | null;
      durationMs: number;
      onDismiss: () => void;
    }

    export default function Toast({ message, durationMs, onDismiss }: ToastProps) {
      useEffect(() => {
        if (!message) return;
        const id = setTimeout(onDismiss, durationMs);
        return () => clearTimeout(id);
      }, [message, durationMs, onDismiss]);

      if (!message) return null;

      return (
        <div
          role="status"
          aria-live="polite"
          aria-atomic="true"
          className="fixed left-1/2 -translate-x-1/2 bottom-8 z-50 px-4 py-3 rounded-xl bg-text-primary/95 text-bg text-sm shadow-lg max-w-[calc(100vw-2rem)] text-center transition-opacity duration-200 opacity-100"
        >
          {message}
        </div>
      );
    }
    ```

    Create `src/components/__tests__/Toast.test.tsx` using vi.useFakeTimers() to control setTimeout deterministically:

    ```tsx
    import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
    import { render, screen, cleanup } from '@testing-library/react';
    import Toast from '../Toast';

    beforeEach(() => {
      vi.useFakeTimers();
    });

    afterEach(() => {
      vi.useRealTimers();
      cleanup();
    });

    describe('Toast', () => {
      it('renders nothing when message is null', () => {
        const { container } = render(<Toast message={null} durationMs={3000} onDismiss={vi.fn()} />);
        expect(container.firstChild).toBeNull();
      });

      it('renders message text with role="status" aria-live="polite" when message is non-null', () => {
        render(<Toast message="Share link copied to clipboard" durationMs={3000} onDismiss={vi.fn()} />);
        const status = screen.getByRole('status');
        expect(status.textContent).toBe('Share link copied to clipboard');
        expect(status.getAttribute('aria-live')).toBe('polite');
        expect(status.getAttribute('aria-atomic')).toBe('true');
      });

      it('auto-dismisses after durationMs by invoking onDismiss', () => {
        const onDismiss = vi.fn();
        render(<Toast message="Hi" durationMs={3000} onDismiss={onDismiss} />);
        expect(onDismiss).not.toHaveBeenCalled();
        vi.advanceTimersByTime(2999);
        expect(onDismiss).not.toHaveBeenCalled();
        vi.advanceTimersByTime(1);
        expect(onDismiss).toHaveBeenCalledTimes(1);
      });

      it('clears the pending timeout when unmounted before durationMs elapses', () => {
        const onDismiss = vi.fn();
        const { unmount } = render(<Toast message="Hi" durationMs={3000} onDismiss={onDismiss} />);
        vi.advanceTimersByTime(1000);
        unmount();
        vi.advanceTimersByTime(5000);
        expect(onDismiss).not.toHaveBeenCalled();
      });

      it('uses the fixed-position bottom-center class string per UI-SPEC L488-491', () => {
        render(<Toast message="Hi" durationMs={3000} onDismiss={vi.fn()} />);
        const status = screen.getByRole('status');
        expect(status.className).toContain('fixed');
        expect(status.className).toContain('bottom-8');
        expect(status.className).toContain('left-1/2');
        expect(status.className).toContain('-translate-x-1/2');
        expect(status.className).toContain('z-50');
      });
    });
    ```
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/Toast.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f src/components/Toast.tsx && test -f src/components/__tests__/Toast.test.tsx`
    - role="status" + aria-live="polite": `grep -E 'role="status"|aria-live="polite"|aria-atomic="true"' src/components/Toast.tsx` returns 3 matches
    - useEffect import + setTimeout pattern: `grep -E "useEffect|setTimeout|clearTimeout" src/components/Toast.tsx` returns ≥ 3 matches
    - Returns null when message is null: `grep "if (!message) return null" src/components/Toast.tsx` returns 1 match
    - Fixed positioning: `grep "fixed left-1/2 -translate-x-1/2 bottom-8 z-50" src/components/Toast.tsx` returns 1 match
    - Auto-dismiss test uses fake timers: `grep "vi.useFakeTimers\|vi.advanceTimersByTime" src/components/__tests__/Toast.test.tsx` returns ≥ 2 matches
    - All tests pass: `npx vitest run src/components/__tests__/Toast.test.tsx` exits 0
    - tsc clean: `npx tsc --noEmit` exits 0
    - Full suite passes: `npx vitest run` exits 0
  </acceptance_criteria>
  <done>
    Toast renders nothing when message is null; renders a live-region div with the locked class string when message is non-null; auto-dismisses by calling onDismiss after durationMs; cleans up its timeout on unmount or message change.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| onClick callback → consumer | Trusted callback supplied by parent (Dashboard in Plan 09-09); no untrusted input |
| message prop → DOM | React's automatic JSX escaping prevents XSS even if message contained '<script>' (defense via React, not the component) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Tampering | CustomCard accidentally modifying PresetCard | mitigate | Parallel-file pattern per CONTEXT.md `canonical_refs` strong preference for parallel CustomCard.tsx (see 09-CONTEXT.md `<canonical_refs>` section); explicit acceptance criterion `git diff --quiet src/components/PresetCard.tsx` proves no diff |
| T-09-03-02 | DoS | Toast leaking setTimeout handles | mitigate | useEffect cleanup returns clearTimeout; test 4 in Task 2 proves it (unmount before durationMs) |
| T-09-03-03 | Information Disclosure | Toast message could leak sensitive content | accept | Messages are hard-coded strings from D-18/D-19 (no PII, no user input rendered) |
| T-09-03-04 | Spoofing | CustomCard '+' glyph misinterpreted by screen readers | mitigate | aria-hidden="true" on the glyph (UI-SPEC L210) + explicit aria-label on the button ("Open custom alarm composer") |
</threat_model>

<verification>
- Both components exist with verbatim UI-SPEC class strings
- Both components pass their unit tests
- PresetCard.tsx byte-identical: `git diff --quiet src/components/PresetCard.tsx` exits 0
- tsc + vitest clean across the new files
- Full test suite still green
</verification>

<success_criteria>
- CustomCard renders the D-09 visual treatment (dashed border, sage '+', "Custom" / "Compose your own alarm")
- CustomCard onClick wires correctly
- Toast renders role=status aria-live=polite with the locked Tailwind class string
- Toast auto-dismisses after durationMs and cleans up its timeout on unmount
- PresetCard.tsx UNCHANGED — parallel-file pattern preserved
- No other protected files modified
</success_criteria>

<output>
After completion, create `.planning/phases/09-custom-composer-share-via-url/09-03-SUMMARY.md` documenting:
- The CustomCard prop signature (onClick: () => void) and the dashed-border deltas from PresetCard
- The Toast prop signature (message, durationMs, onDismiss) and ARIA contract
- Test counts for each file
- Confirmation PresetCard.tsx remained byte-identical
</output>
