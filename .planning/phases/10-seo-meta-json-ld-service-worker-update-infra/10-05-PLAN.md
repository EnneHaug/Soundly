---
phase: 10-seo-meta-json-ld-service-worker-update-infra
plan: 05
type: execute
wave: 2
depends_on: [10-01, 10-02, 10-03, 10-04]
files_modified:
  - docs/deploy-runbook.md
autonomous: true
requirements: [SEO-06, SEO-07, SEO-08, SEO-09]
tags: [docs, deploy, runbook, search-console]

must_haves:
  truths:
    - "`docs/deploy-runbook.md` exists with Pre-deploy / Deploy / Post-deploy sections (D-28)"
    - "Placeholder URL swap step documented with exact find/replace commands (D-23 + RESEARCH Finding 7)"
    - "OG image regeneration + D-09 approval flow documented"
    - "Search Console manual sitemap submission step documented (D-28 step 4) as the primary discoverability path on GitHub Pages (per v2.0 Hosting decision)"
    - "Facebook Sharing Debugger force-refresh step documented for OG image content changes (D-28 step 5, SEO-08)"
    - "SW update smoke-test step documented (RESEARCH Finding 10) — the only realistic end-to-end verification of SEO-09 behavior"
    - "Google Rich Results Test 'not eligible for rich results' caveat documented (Pitfall 6) so future maintainers don't chase a non-bug"
    - "`</script>` JSON-LD maintenance rule documented (Pitfall 4)"
  artifacts:
    - path: "docs/deploy-runbook.md"
      provides: "Operator playbook for canonical-URL swap, OG approval, deploy, Search Console submission, SW update smoke test"
      contains: "soundly.local, sed, og-image, Search Console, sitemap.xml, Facebook Sharing Debugger, skipWaiting, clientsClaim"
      min_lines: 60
  key_links:
    - from: "docs/deploy-runbook.md pre-deploy section"
      to: "index.html + public/robots.txt + public/sitemap.xml (Plans 01 + 02)"
      via: "documented sed find/replace command"
      pattern: "soundly\\.local"
    - from: "docs/deploy-runbook.md post-deploy section"
      to: "Schema.org Validator + Facebook Sharing Debugger + Search Console"
      via: "documented external-validator URLs"
      pattern: "validator\\.schema\\.org|developers\\.facebook\\.com|search\\.google\\.com/search-console"
---

<objective>
Author `docs/deploy-runbook.md` — the operator playbook that downstream plans (01, 02, 03, 04) reference. It documents:
- The find/replace canonical-URL swap (the load-bearing manual step from D-23)
- OG image regeneration + D-09 approval flow (cross-reference to Plan 04)
- The `npm run build` verification commands (mirrors Plan 06's automated tests)
- Search Console manual sitemap submission (primary discoverability path on GitHub Pages per v2.0 Hosting decision)
- Facebook Sharing Debugger force-refresh for OG changes
- SW update smoke test (the only end-to-end verification of SEO-09 per RESEARCH Finding 10)
- The Pitfall 6 Google Rich Results Test caveat
- The Pitfall 4 JSON-LD `</script>` maintenance rule

Purpose: Closes the D-28 LOCKED requirement. Without this runbook, the user has six committed files with placeholder URLs and no recorded procedure for swapping them, submitting the sitemap, or verifying the SW update.

Output: One new Markdown file in a new `docs/` top-level directory.

Depends on Plans 01-04 so the documented file paths + commands are accurate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-seo-meta-json-ld-service-worker-update-infra/10-CONTEXT.md
@.planning/phases/10-seo-meta-json-ld-service-worker-update-infra/10-RESEARCH.md
@.planning/phases/10-seo-meta-json-ld-service-worker-update-infra/10-PATTERNS.md
@.planning/phases/10-seo-meta-json-ld-service-worker-update-infra/10-01-PLAN.md
@.planning/phases/10-seo-meta-json-ld-service-worker-update-infra/10-02-PLAN.md
@.planning/phases/10-seo-meta-json-ld-service-worker-update-infra/10-03-PLAN.md
@.planning/phases/10-seo-meta-json-ld-service-worker-update-infra/10-04-PLAN.md

<interfaces>
<!-- The 6 placeholder canonical-URL occurrences (these are what the runbook documents swapping):
     - index.html (5x): canonical link, og:url, og:image, twitter:image, json-ld url + og:image
     - public/robots.txt (1x): Sitemap: line
     - public/sitemap.xml (2x): both <loc> elements
     Total: 8 substring occurrences across 3 files (all matching the literal `https://soundly.local/Soundly/`).
     Single sed/find-replace removes all 8 in one shot. -->

<!-- This is the first top-level `docs/` directory in the repo. PATTERNS section notes:
     "No existing in-repo Markdown documentation beyond a 1-line README.md and the .planning/ workflow files."
     The runbook MAY be linked from README.md in a future polish phase; not required for Phase 10. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/deploy-runbook.md (D-28 LOCKED content)</name>
  <files>docs/deploy-runbook.md</files>
  <read_first>
    - .planning/phases/10-seo-meta-json-ld-service-worker-update-infra/10-CONTEXT.md (`<decisions>` D-23, D-28, AND `<specifics>` for verbatim file contents)
    - .planning/phases/10-seo-meta-json-ld-service-worker-update-infra/10-RESEARCH.md (Finding 6 GH Pages constraint, Finding 7 URL swap strategy, Finding 10 SW smoke test, Finding 11 runbook structure, Pitfalls 3/4/6)
    - .planning/phases/10-seo-meta-json-ld-service-worker-update-infra/10-PATTERNS.md (section "`docs/deploy-runbook.md` — new")
    - .planning/phases/10-seo-meta-json-ld-service-worker-update-infra/10-01-PLAN.md (for canonical placement of `https://soundly.local/Soundly/`)
    - .planning/phases/10-seo-meta-json-ld-service-worker-update-infra/10-02-PLAN.md (for robots.txt + sitemap.xml exact paths)
    - .planning/phases/10-seo-meta-json-ld-service-worker-update-infra/10-04-PLAN.md (for og-image regeneration command)
  </read_first>
  <action>
    Create `docs/deploy-runbook.md` (new file, new top-level `docs/` directory).

    Write the file with these exact contents (ATX `#` headers, GFM checklists, fenced code blocks with language tags). This is a self-contained operator playbook; every step must be runnable as-written by a developer who has not read the planning artifacts:

    ````markdown
    # Soundly Deploy Runbook

    Operator playbook for shipping Soundly to production (GitHub Pages or
    equivalent static-host). Covers Phase 10 SEO + SW update artifacts.

    > **Read first:** This runbook assumes Phase 10 has shipped — `index.html`,
    > `public/robots.txt`, `public/sitemap.xml`, `public/og-image-v1.png`,
    > `vite.config.ts`, and `src/sw.ts` are all in the v2.0 state described
    > in `.planning/phases/10-seo-meta-json-ld-service-worker-update-infra/`.

    ---

    ## 1. Pre-deploy: swap placeholder canonical URL

    All committed SEO files use the literal placeholder string
    `https://soundly.local/Soundly/`. Before deploying, replace it with your
    actual deploy URL across three files.

    **Three files, six occurrences:**

    | File | Occurrences | What gets replaced |
    |------|-------------|--------------------|
    | `index.html` | 5 (canonical link, og:url, og:image, twitter:image, JSON-LD url, og:image) | All `https://soundly.local/Soundly/...` substrings |
    | `public/robots.txt` | 1 (Sitemap: line) | The full sitemap URL |
    | `public/sitemap.xml` | 2 (both `<loc>` elements) | Both URLs |

    **Single-command swap (POSIX shell):**

    ```bash
    # 1. Set your actual deploy URL (MUST end with trailing slash + match the Vite `base` from vite.config.ts)
    NEW_URL="https://username.github.io/Soundly/"

    # 2. Find/replace across the three files (creates .bak files; cleaned up below)
    sed -i.bak "s|https://soundly.local/Soundly/|${NEW_URL}|g" \
      index.html public/robots.txt public/sitemap.xml

    # 3. Clean up sed backup files
    rm -f index.html.bak public/robots.txt.bak public/sitemap.xml.bak

    # 4. Sanity check — should print NO matches
    grep -r "soundly.local" . --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning
    ```

    **PowerShell equivalent (Windows):**

    ```powershell
    $NEW_URL = "https://username.github.io/Soundly/"
    foreach ($file in @("index.html", "public/robots.txt", "public/sitemap.xml")) {
      (Get-Content $file -Raw) -replace 'https://soundly\.local/Soundly/', $NEW_URL | Set-Content $file -NoNewline
    }
    # Sanity check — should return nothing
    Select-String -Path "index.html","public/robots.txt","public/sitemap.xml" -Pattern "soundly.local"
    ```

    **DO NOT commit the swapped state to main.** Make the swap on the deploy
    branch / in CI / in a release commit only. The `soundly.local` placeholder
    in main remains the source of truth so future contributors see the
    deliberate placeholder pattern.

    ---

    ## 2. Pre-deploy: generate / accept OG image

    Only when content changes; otherwise skip to step 3.

    ```bash
    npm run og-image    # writes public/og-image-v1.png (1200×630, ≤ 200 KB)
    ```

    Open `public/og-image-v1.png` in any image viewer:
    - Windows: double-click in File Explorer (default Photos app)
    - macOS: Preview.app
    - VS Code: open the file directly (inline preview)

    **Acceptance criteria:**
    - Layout balanced (ring + wordmark + tagline)
    - Typography readable at chat-preview size
    - On-brand warm-earth palette only — no jarring colors

    If unacceptable: edit `scripts/generate-og-image.mjs` (palette constants
    + SVG composition near the top), re-run `npm run og-image`, re-inspect.

    Iterate until satisfied, then `git add public/og-image-v1.png` and proceed.

    **When changing IMAGE CONTENT (not just regenerating same composition):**
    bump the filename to `og-image-v2.png` (and update references in
    `index.html`). Reason: Facebook caches OG image URLs aggressively
    (~30 days). Same URL = same cache entry = stale preview for everyone
    who shared the URL pre-update. New filename forces fresh scrapes.
    See SEO-08 / D-10 / Pitfall 3.

    ---

    ## 3. Build

    ```bash
    npm run build
    ```

    Verify after build (sanity gates — Plan 06 automates these):

    ```bash
    # Static meta in dist/index.html
    grep -F 'og:title' dist/index.html              # expect 1+ match
    grep -F 'og:description' dist/index.html        # expect 1+ match
    grep -F 'twitter:card' dist/index.html          # expect 1+ match
    grep -F 'rel="canonical"' dist/index.html       # expect 1+ match
    grep -F 'application/ld+json' dist/index.html   # expect 1+ match
    grep -F '"WebApplication"' dist/index.html      # expect 1+ match

    # Static asset files present
    test -f dist/og-image-v1.png && echo "og-image OK"
    test -f dist/robots.txt && echo "robots OK"
    test -f dist/sitemap.xml && echo "sitemap OK"

    # OG image size ≤ 200 KB (POSIX)
    test $(stat -c%s dist/og-image-v1.png 2>/dev/null || stat -f%z dist/og-image-v1.png) -le 204800 && echo "OG size OK"
    ```

    **PowerShell OG size check:**

    ```powershell
    $size = (Get-Item dist/og-image-v1.png).Length
    if ($size -le 204800) { Write-Output "OG size OK ($size bytes)" } else { Write-Error "OG image exceeds 200 KB: $size bytes" }
    ```

    ---

    ## 4. Deploy

    Deploy mechanism is implementation-defined (GitHub Pages workflow, manual
    upload, etc.). Phase 10 does NOT lock the deploy step — only the artifacts.

    For GitHub Pages with Actions: push the contents of `dist/` to the
    `gh-pages` branch (or use `peaceiris/actions-gh-pages`). Verify the
    deployment by visiting `https://<NEW_URL>` (the URL set in step 1).

    ---

    ## 5. Post-deploy: verify static SEO meta

    1. **`view-source:` (NOT DevTools Elements)** in your browser:
       - Navigate to `view-source:https://<your-deploy-url>/`
       - Confirm: title, description, all OG tags, Twitter tags, canonical
         link, and JSON-LD block appear as PLAIN TEXT in the page source
       - **DevTools Elements is NOT a valid check.** It shows the
         post-React-hydration DOM, which includes everything you might
         add later via JS. Scrapers run zero JS — `view-source:` is what
         they see. (Pitfall 1.)

    2. **Schema.org Validator** — https://validator.schema.org/
       - Paste your deployed URL → expect **zero errors**
       - The WebApplication block should be detected as a valid
         SoftwareApplication subtype

    3. **Google Rich Results Test** — https://search.google.com/test/rich-results
       - Paste your deployed URL → expect "Page is not eligible for rich results"
       - **This is NOT an error.** WebApplication / SoftwareApplication is
         NOT in Google's rich-result-eligible types list (rich-result-eligible
         types are limited: Product, Recipe, Article, etc.). The schema is
         valid; it just won't render with stars/ratings in search snippets.
         (Pitfall 6.)

    ---

    ## 6. Post-deploy: Search Console manual sitemap submission

    GitHub Pages does NOT auto-discover sitemaps at subpath sites. Manual
    submission via Google Search Console is the **primary discoverability
    path** for the Soundly deploy (per v2.0 ROADMAP "Hosting" decision).

    1. Sign in to https://search.google.com/search-console
    2. Add your deploy URL as a property (URL-prefix property:
       `https://<your-deploy-url>/`)
    3. Verify ownership (Search Console offers several methods — DNS TXT
       record, HTML file upload, or `<meta name="google-site-verification">`
       in `index.html`. The meta-tag method works without DNS access — paste
       Google's verification meta tag into `index.html` after the existing
       SEO meta block.)
    4. Navigate to "Sitemaps" in the left nav
    5. Enter `sitemap.xml` (the path relative to your property root) → Submit
    6. Wait — initial indexing can take days/weeks

    Note: the `/app/` URL in the sitemap will return 404 until Phase 11
    ships. Search Console will flag it as "discovered but not indexed" —
    this is the expected forward-compat tradeoff (D-21).

    ---

    ## 7. Post-deploy: force-refresh Facebook Sharing Debugger

    Required when:
    - You've deployed `index.html` with new OG copy (title/description)
    - You've swapped the OG image content at the same URL (`og-image-v1.png`)
    - You've bumped to a new versioned OG image (`og-image-v2.png`) and want
      existing shared links to render the new image

    1. Visit https://developers.facebook.com/tools/debug/
    2. Paste your deployed URL → Click "Debug"
    3. Click "Scrape Again" to force fresh re-fetch
    4. Verify the OG title, description, and image preview render correctly
    5. The preview chip shown is what Facebook + Instagram + Messenger
       + Slack (via OG-spec consumption) + iMessage (via Twitter-card
       fallback) will display

    See SEO-08 / D-10 for the versioned-filename rationale.

    ---

    ## 8. Post-deploy: SW update smoke test (when meta or SW changes)

    The full end-to-end verification of SEO-09 (service worker auto-update)
    is structurally NOT automatable — it requires a real browser, a real
    install, and a real cross-deploy lifecycle (RESEARCH Finding 10).

    Manual smoke test:

    1. On a phone with the v1 PWA installed (e.g., a device where you
       installed Soundly before deploying v2):
       - Close the app fully (swipe away from app switcher)
       - Reopen the app
       - Expected: the v2 meta is now active (the new SW takes over via
         `skipWaiting()` + `clientsClaim()` from `src/sw.ts`)
    2. Most direct verification:
       - Temporarily change `<title>` in `index.html` to a clearly-recognizable
         string like `Soundly v2 test`
       - Re-run swap → build → deploy
       - On the phone, close + reopen the PWA — confirm the new title shows
         in the app-switcher chrome (or in mobile Safari/Chrome tab title)
       - Revert the `<title>` change → swap → build → deploy

    If the smoke test FAILS (installed PWA still shows old meta):
    - Verify both `registerType: 'autoUpdate'` (vite.config.ts) AND
      `self.skipWaiting()` + `clientsClaim()` (src/sw.ts) are present —
      Pitfall 2 (both must land together; neither alone delivers updates)
    - Verify the new build was actually deployed (check `dist/sw.ts.map`
      or the SW file in the deployed `/Soundly/sw.js` URL)
    - On the phone, force-close the app fully (not just background it) —
      iOS especially keeps inactive SW workers alive longer than expected

    ---

    ## 9. Maintenance notes

    ### Bumping the sitemap `<lastmod>` date

    When meta or content changes meaningfully, manually update both `<lastmod>`
    elements in `public/sitemap.xml` to today's date (YYYY-MM-DD). No
    automation; intentional manual gate per D-22.

    ### Editing JSON-LD `description` content

    Never include the literal substring `</script>` inside the JSON-LD
    `description` field. The HTML parser tokenizes `</script>` as the end
    of the script element, regardless of JSON context — your JSON-LD block
    will terminate prematurely and the rest of the JSON becomes malformed
    markup. If you must include script-tag references, escape as
    `<\/script>`. (Pitfall 4.)

    ### Upgrading vite-plugin-pwa

    The project pins `vite-plugin-pwa@^0.21.1`. Do NOT bump to 1.x — breaking
    config-schema changes risk regressing the validated v1.0 SW. If an
    upgrade is needed, treat it as its own phase with a full validation
    matrix. (Pitfall 5 + REQUIREMENTS.md Out-of-Scope table.)

    ### Adding new OG image versions

    Always bump the filename (`og-image-v1.png` → `og-image-v2.png` →
    `og-image-v3.png`). Update both `index.html` references (og:image,
    twitter:image) to the new filename. The old file MAY remain in `public/`
    for back-compat with pre-bump shared links, or MAY be removed after a
    transition period.

    ---

    *Phase 10 deploy runbook. Updated: see `git log -1 -- docs/deploy-runbook.md`.*
    ````

    The runbook is intentionally long (~200 lines) — it has to be self-contained because the user comes back to it months after Phase 10 ships and needs every step at hand without re-reading planning artifacts.

    **Do NOT add a `## Quickstart` TL;DR** — the linear step ordering IS the quickstart. A second summary list invites drift.

    **Do NOT include deployment commands for specific hosts** (e.g., Cloudflare Pages, Netlify) — Phase 10 doesn't lock the deploy mechanism. The GitHub Pages reference in step 4 is one example, not a recommendation.

    **Do NOT cross-link to `.planning/` artifacts in the user-facing runbook** — the runbook should be readable standalone by an operator who has never touched `.planning/`. The only `.planning/` reference is in the "Read first" header note.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const md=fs.readFileSync('docs/deploy-runbook.md','utf8'); const checks={'soundly.local placeholder':/soundly\.local/,'sed command':/sed -i\.bak/,'PowerShell variant':/Get-Content/,'og-image npm script':/npm run og-image/,'sharing debugger URL':/developers\.facebook\.com\/tools\/debug/,'search console URL':/search\.google\.com\/search-console/,'schema validator URL':/validator\.schema\.org/,'rich results URL':/search\.google\.com\/test\/rich-results/,'view-source warning':/view-source:/,'DevTools warning':/DevTools Elements/i,'Pitfall 4 JSON-LD warning':/<\\\\\/script>|<\/script>/,'SW smoke test':/skipWaiting|clientsClaim/,'200 KB cap':/200 KB|204800/,'forward-compat /app note':/forward-compat|expected forward-compat|D-21/,'rich-result-eligible caveat':/not eligible for rich results/i,'sitemap manual submission':/Search Console.*[Mm]anual|[Mm]anual.*[Ss]itemap/,'vite-plugin-pwa pin warning':/vite-plugin-pwa.*0\.21|0\.21.*vite-plugin-pwa|1\.x/}; const missing=Object.entries(checks).filter(([k,r])=>!r.test(md)).map(([k])=>k); if(missing.length)throw new Error('Missing: '+missing.join(', ')); const lc=md.split(/\\r?\\n/).length; if(lc<60)throw new Error('Runbook too short: '+lc+' lines (expected ≥60)'); console.log('OK — '+lc+' lines')"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/deploy-runbook.md` exists
    - File is ≥ 60 lines (operator playbook needs to be self-contained)
    - Contains the placeholder URL `soundly.local` (so the sed/PowerShell commands are concrete)
    - Contains both `sed -i.bak` (POSIX) AND `Get-Content` (PowerShell) variants of the swap
    - Documents `npm run og-image` regeneration command
    - Documents Facebook Sharing Debugger URL (https://developers.facebook.com/tools/debug/)
    - Documents Google Search Console URL (https://search.google.com/search-console)
    - Documents Schema.org Validator URL (https://validator.schema.org/)
    - Documents Google Rich Results Test URL + the "not eligible for rich results" caveat (Pitfall 6)
    - Documents the `view-source:` vs DevTools Elements distinction (Pitfall 1)
    - Documents the JSON-LD `</script>` maintenance rule (Pitfall 4)
    - Documents the SW update smoke test referencing `skipWaiting` / `clientsClaim`
    - Documents the 200 KB OG image cap (204800 bytes)
    - Documents the `/app/` 404 forward-compat tradeoff (D-21)
    - Documents the vite-plugin-pwa version pin warning (Pitfall 5)
    - Documents the manual sitemap submission as PRIMARY path (per v2.0 Hosting decision)
  </acceptance_criteria>
  <done>
    `docs/deploy-runbook.md` exists, ≥ 60 lines, covers all 9 numbered sections (pre-deploy URL swap, OG image, build, deploy, view-source verify, Search Console, Facebook Debugger, SW smoke test, maintenance). All 17 verifier checks pass. Operator can ship Phase 10 deploy from this doc alone.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/deploy-runbook.md → operator | Operator follows commands literally. Wrong commands in the runbook = wrong deploy. |
| Runbook content → public read | The runbook is in `docs/` which is committed; anyone with repo access reads it. No secrets included. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-01 | Tampering | runbook command examples | mitigate | Verifier asserts presence of both `sed -i.bak` (POSIX) AND `Get-Content` (PowerShell) variants — operator gets a working command for their shell. URL placeholder is concrete (`soundly.local`) so find/replace command is literal, not abstract. |
| T-10-05-02 | Information Disclosure | runbook documents internal procedures | accept | Solo-dev project; runbook is public-friendly documentation of public procedures (Search Console submission, Facebook Debugger usage). No credentials, no secrets. |
| T-10-05-03 | Spoofing | runbook recommends external services | accept | Schema.org, Google Search Console, Facebook Sharing Debugger are well-known canonical services; URL typos would fail to load (immediately detectable). Verifier asserts the URLs exactly. |
| T-10-05-04 | Denial of Service | bad sed command corrupts files | mitigate | Runbook uses `-i.bak` (POSIX) which creates backup files; sanity-check grep step (`grep -r "soundly.local"`) verifies the swap succeeded before deploy. |
| T-10-05-05 | Repudiation | runbook changes over time | accept | Git history tracks runbook edits; footer notes `git log -1 -- docs/deploy-runbook.md` for currency. |
| T-10-05-06 | Elevation of Privilege | runbook misleads operator | mitigate | Pitfall 1 (view-source vs DevTools) + Pitfall 6 (Rich Results "not eligible") + Pitfall 4 (JSON-LD `</script>`) explicitly documented — operator can't accidentally chase the wrong failure. |
</threat_model>

<verification>
- `node -e "..."` verifier asserts all 17 required content fragments present + minimum line count
- Runbook is human-readable (sections numbered 1-9, ATX headers, fenced code blocks)
- Runbook contains both POSIX + PowerShell command variants for cross-platform operator support
- No secrets / credentials in the file (verifier indirectly — only documents public URLs)
</verification>

<success_criteria>
- [ ] `docs/deploy-runbook.md` exists, ≥ 60 lines
- [ ] All 9 numbered sections present (pre-deploy URL swap, OG image, build, deploy, view-source verify, Search Console, Facebook Debugger, SW smoke test, maintenance)
- [ ] All 4 pitfalls cross-referenced (Pitfall 1, 3, 4, 6)
- [ ] Both POSIX + PowerShell variants of the URL swap command present
- [ ] All 4 external validator URLs present (Schema.org, Rich Results, Search Console, Facebook Debugger)
- [ ] Vite-plugin-pwa version pin warning documented
- [ ] `/app/` forward-compat tradeoff documented
- [ ] Automated verifier exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/10-seo-meta-json-ld-service-worker-update-infra/10-05-SUMMARY.md` documenting:
- File path + line count
- The 9 section titles
- Confirmation that all 17 required content fragments are present (per verifier checks)
- Note that this runbook closes the D-28 LOCKED requirement and is the load-bearing manual step for SEO-06 (Sitemap submission), SEO-08 (FB Debugger force-refresh), SEO-09 (SW update smoke test)
</output>
