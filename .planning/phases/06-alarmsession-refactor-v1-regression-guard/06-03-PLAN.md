---
phase: 06-alarmsession-refactor-v1-regression-guard
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - .planning/phases/06-alarmsession-refactor-v1-regression-guard/06-REGRESSION-CHECKLIST.md
autonomous: true
requirements:
  - SEG-05
tags:
  - documentation
  - regression
  - testing
must_haves:
  truths:
    - "06-REGRESSION-CHECKLIST.md exists at the phase directory and documents the on-device manual verification matrix"
    - "Checklist covers Quick Nap on desktop Chrome, Quick Nap on Android Chrome, Focus on desktop Chrome, pause/resume across phases 1-3, and iOS Safari best-effort coverage (per CONTEXT specifics)"
    - "Each section uses a checkbox-list format suitable for an operator to mark off during a real device run"
    - "iOS Safari section is explicitly framed as best-effort / expected partial — the locked-screen limitation is acknowledged and deferred to LAND-05"
    - "The checklist delivers Phase 6 success criterion #4 (a documented v1 regression check) and is the secondary gate per D-07 (automated tests are primary; this is documentation)"
  artifacts:
    - path: ".planning/phases/06-alarmsession-refactor-v1-regression-guard/06-REGRESSION-CHECKLIST.md"
      provides: "Manual on-device regression matrix for v1 byte-identity verification"
      contains: "Quick Nap"
      contains_also: "Focus"
      contains_also_2: "Pause"
      min_lines: 60
  key_links:
    - from: "06-REGRESSION-CHECKLIST.md"
      to: ".planning/ROADMAP.md Phase 6 success criterion #4"
      via: "documents the on-device matrix that proves v1 paths still work"
      pattern: "Quick Nap"
    - from: "06-REGRESSION-CHECKLIST.md"
      to: "REQUIREMENTS.md SEG-05"
      via: "exercises the SEG-05 zero-diff floor on real devices"
      pattern: "SEG-05"
---

<objective>
Author `06-REGRESSION-CHECKLIST.md` — a markdown checklist that captures the on-device manual regression matrix for v1 byte-identity. This delivers Phase 6 success criterion #4 ("A documented v1 regression check passes…") and is the secondary verification gate per D-07 (automated tests in Plan 2 are the primary gate; this checklist exists as documentation and is recommended-but-not-required to run as a CI gate).

Purpose: Phase 6 is a refactor with zero new user-visible behavior. The existing automated tests (extended in Plan 2) catch most regressions, but some — vibration patterns on real Android, Wake Lock holding the screen on a real phone, audio actually being audible at the right time — only show up on physical devices. This checklist exists so that the human operator (the user) has a deterministic procedure to run when they want to validate Phase 6 before merging.

Output: a single markdown file at `.planning/phases/06-alarmsession-refactor-v1-regression-guard/06-REGRESSION-CHECKLIST.md` with five sections corresponding to the device matrix in CONTEXT.md `<specifics>` lines 189–195. No source code is touched. This plan is independent of Plan 2 and can run in parallel with it (no shared files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-alarmsession-refactor-v1-regression-guard/06-CONTEXT.md
@CLAUDE.md

<key_decisions>
- D-07 primary vs secondary: automated tests (Plan 2) are the CI gate. This checklist is the SECONDARY documentation gate — written but not required to run for CI. The user runs it on demand when they want device confidence before shipping.
- SEG-05: the checklist's purpose is to prove byte-identical v1 behavior on the user-visible surface (Quick Nap, Focus, pause/resume). It does NOT cover Phase 7+ features.
- iOS scope (per `<specifics>` line 195 + LAND-05): iOS coverage is best-effort. The locked-screen audio limitation is structural and is acknowledged separately in v2.0 LAND-05; this checklist neither hides nor over-promises iOS reliability.
</key_decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 06-REGRESSION-CHECKLIST.md with the five-section device matrix</name>
  <files>.planning/phases/06-alarmsession-refactor-v1-regression-guard/06-REGRESSION-CHECKLIST.md</files>

  <read_first>
    - .planning/phases/06-alarmsession-refactor-v1-regression-guard/06-CONTEXT.md (sections: `<specifics>` lines 189–195 — the suggested five sections; `<decisions>` D-07 — primary vs secondary gate; `<deferred>` — items NOT to surface as Phase 6 regressions)
    - .planning/ROADMAP.md (Phase 6 §"Success Criteria" #4 — the criterion this checklist documents)
    - .planning/REQUIREMENTS.md (SEG-05 — the zero-diff floor the on-device matrix exercises)
    - CLAUDE.md (Wake Lock + Vibration API + Notifications API caveats — informs the device-matrix expectations)
  </read_first>

  <action>
Create `.planning/phases/06-alarmsession-refactor-v1-regression-guard/06-REGRESSION-CHECKLIST.md` as a plain markdown file. Use horizontal rules sparingly (a single one between major sections at most) — the section headings (`## Section 1`, `## Section 2`, etc.) provide enough visual separation. Cover all five sections, all listed checkboxes, and the iOS expectation framing.

Required content blueprint (executor MAY refine wording but MUST cover every listed item):

**Title and intro:**
- H1 heading: `# Phase 6: v1 Regression Checklist (On-Device Manual Matrix)`.
- "Purpose" paragraph stating this verifies the AlarmSession refactor preserves v1 byte-identical behavior for Quick Nap and Focus on real devices.
- "Status" paragraph stating this is the SECONDARY verification gate per D-07; the PRIMARY gate is the automated test suite (`npx vitest run`, extended in Plan 2). Recommended-but-not-required to run before merging Phase 6.
- "SEG-05 reminder" paragraph stating Phase 6 is a pure refactor; any user-visible behavior change observed during this checklist is a regression and must be addressed before merge.

**"How to use" subsection:**
- Build and run the dev or production app on the target device.
- Walk each section in order; mark each checkbox as you verify it.
- If any item fails, file a regression note in the phase directory and DO NOT merge.
- iOS Safari has known structural limitations (Section 5) — partial pass is expected and acceptable.

**Section 1 — Quick Nap on desktop Chrome.** Target: Chrome (latest stable) on macOS or Windows desktop, headphones recommended. Checkboxes for: tap Quick Nap → countdown appears; after Phase 1 duration the singing-bowl strike plays once and is audible; after Phase 2 duration the tick pulse begins (no Vibration API on desktop); the tick pulse audibly ramps over Phase 2 duration; after Phase 2 + gap the Phase 3 swell begins with a 0%→100% ramp; during the entire timer the screen does not dim or sleep; tap Stop → audio ceases and screen sleep returns to normal; relaunch immediately and confirm a clean start.

**Section 2 — Quick Nap on Android Chrome.** Target: Chrome on Android with vibration enabled in OS settings. Checkboxes for: tap Quick Nap → countdown appears; Phase 1 strike plays once; Phase 2 vibrates per the configured pattern (no tick pulse — Vibration API code path); screen stays on during the timer; Phase 3 ramp is audible; Stop halts vibration immediately and ends Phase 3 audio; backgrounding the tab during Phase 1 and returning re-acquires the Wake Lock on visibilitychange.

**Section 3 — Focus on desktop Chrome.** Target: same as Section 1; Focus runs at 21 min Phase 1 / 2 min Phase 2 cadence. Checkboxes for: tap Focus → countdown appears; Phase 1 strike at 21:00; Phase 2 begins at 23:00 and ramps over the 2-minute Phase 2 duration; Phase 3 begins after the gap and ramps; Wake Lock held throughout; Stop returns engine to idle; relaunch is clean. Add an optional-shortcut paragraph noting the operator MAY temporarily edit `QUICK_NAP_CONFIG` / `FOCUS_CONFIG` in `src/engine/AlarmState.ts` for faster iteration but MUST NOT commit those changes — SEG-05 forbids any diff to AlarmState.ts.

**Section 4 — Pause / Resume across Phase 1 / 2 / 3.** Target: any platform from Sections 1 or 2. Checkboxes for: launch Quick Nap → wait until Phase 1 audio is playing; tap Pause → audio/tick/vibration ceases and countdown stops advancing; wait 5 seconds → tap Resume → audio/tick/vibration resumes from the same logical position; total elapsed time at Phase 2 fire equals original Phase 1 + Phase 2 plus the 5-second pause; repeat the pause/resume during Phase 2; repeat during Phase 3 (resume must continue the swell loop without a click or restart artifact); displayed countdown reflects the pause-extended total in all cases.

**Section 5 — iOS Safari (best-effort — expected partial pass).** Target: Safari on iPhone, iOS 17+, headphones recommended. Open with a blockquote (`>` lines) framing this as best-effort: no Vibration API support (tick fallback is by design — ALM-05); AudioContext may suspend on screen lock even with keepalive running; Wake Lock support varies by iOS version; the structural locked-screen limitation is acknowledged in v2.0 LAND-05 and is NOT a Phase 6 regression. Then list checkboxes for foreground-only verification: launch Quick Nap from foregrounded app → Phase 1 strike fires; Phase 2 plays the tick pulse (no vibration); Phase 3 ramp plays while foregrounded; Wake Lock holds the screen on (verify on iOS 18+; older versions may not); Stop halts audio and relaunch is clean. Close the section with a one-liner stating locked-screen behavior is OUT OF SCOPE for Phase 6 and documented in LAND-05 (Phase 11).

**Sign-off:**
- Heading: `## Sign-off`.
- One paragraph: "If all sections (1–4) pass and Section 5 passes its foregrounded checks: Phase 6 is verified for v1 byte-identity on real devices."
- Three blank fields with underscores for: Operator, Date, Build / commit. Use the format `**Operator:** _________________________` (bold label + underscore underline) — do NOT use the `key: value` form (which can confuse YAML parsers if a future tool re-scans the file as multi-document YAML).

**Footer:**
- A horizontal-rule-free trailing italics block with: phase identifier, checklist-authored date (2026-05-07), and a note that the primary verification gate is `npx vitest run` (see 06-02-PLAN.md).

Implementation rules (follow strictly):
1. The file MUST live at the exact path `.planning/phases/06-alarmsession-refactor-v1-regression-guard/06-REGRESSION-CHECKLIST.md`. No other location.
2. Use plain markdown — NO YAML frontmatter (the file is a checklist, not a planning artifact).
3. Use the `- [ ]` GitHub-flavored checkbox syntax for every item the operator marks off.
4. Sections 1–5 MUST appear, in order, with `## Section N — ...` headings exactly as listed above.
5. Section 5 MUST contain a blockquote (`>`) framing it as best-effort and mention LAND-05 as the place where the iOS locked-screen limitation lives.
6. The optional-shortcut paragraph in Section 3 MUST explicitly say "DO NOT commit" the AlarmState.ts edit — SEG-05 enforcement.
7. The "How to use" preamble MUST mention that automated tests / `npx vitest run` are the primary gate and this checklist is secondary.
8. The Sign-off block MUST use the `**Operator:** ___` (bold label, underscores) format — NOT `Operator: ___` — to avoid creating lines that look like YAML keys to downstream tooling that may re-scan markdown for frontmatter.
9. Use AT MOST one horizontal rule (`---`) line between major sections (and only if needed for visual separation). Do NOT bracket sections with multiple `---` separators — they can confuse multi-document YAML parsers that scan markdown files.
  </action>

  <verify>
    <automated>test -f .planning/phases/06-alarmsession-refactor-v1-regression-guard/06-REGRESSION-CHECKLIST.md && wc -l .planning/phases/06-alarmsession-refactor-v1-regression-guard/06-REGRESSION-CHECKLIST.md | awk '{ exit ($1 >= 60 ? 0 : 1) }'</automated>
  </verify>

  <acceptance_criteria>
    - File `.planning/phases/06-alarmsession-refactor-v1-regression-guard/06-REGRESSION-CHECKLIST.md` exists.
    - File is at least 60 non-empty lines.
    - File contains the literal heading `## Section 1 — Quick Nap on desktop Chrome`.
    - File contains the literal heading `## Section 2 — Quick Nap on Android Chrome`.
    - File contains the literal heading `## Section 3 — Focus on desktop Chrome`.
    - File contains the literal heading `## Section 4 — Pause / Resume across Phase 1 / 2 / 3`.
    - File contains the literal heading `## Section 5 — iOS Safari (best-effort — expected partial pass)`.
    - File contains at least 20 GitHub-flavored checkboxes (`- [ ]`) across all sections combined.
    - File contains the literal text `SEG-05` at least once.
    - File contains the literal text `LAND-05` at least once.
    - File contains the literal text `npx vitest` (or `automated tests`) somewhere in the preamble.
    - File contains the literal text `DO NOT commit` somewhere in Section 3 (SEG-05 enforcement on the optional-shortcut shortcut).
    - File uses `**Operator:**` (bold label) rather than the bare `Operator:` form in the Sign-off block (grep `^Operator:` returns zero matches; grep `\*\*Operator:\*\*` returns at least one match).
    - File contains AT MOST 2 standalone `---` lines (the YAML-document-separator pattern is avoided).
    - SEG-05 zero-diff guard: `git diff --name-only HEAD -- src/` produces zero output from this plan alone (this plan touches no source file).
  </acceptance_criteria>

  <done>
The file `06-REGRESSION-CHECKLIST.md` exists at the phase directory with five sections (Quick Nap desktop / Quick Nap Android / Focus desktop / Pause-Resume / iOS best-effort), at least 20 checkboxes, the D-07 primary-vs-secondary framing, explicit cross-links to SEG-05 and LAND-05, and a Sign-off block that uses the `**Operator:**` bold-label form. No source file is modified by this plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — documentation-only plan) | This plan touches no source code, no public API, no runtime path. |

This plan is documentation-only. It introduces no executable surface, no input boundary, no auth flow. STRIDE applicability is minimal but the block is mandatory per `<security_threat_model_requirement>`.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Information Disclosure | checklist references implementation details that could mislead | accept | The checklist references public file paths (e.g., `src/engine/AlarmState.ts`) that are already visible in the public repo. No secrets, no credentials, no internal-only details. LOW. |
| T-06-14 | Tampering | operator follows checklist with stale info, misses a real regression | mitigate | Checklist explicitly frames itself as the SECONDARY gate, with `npx vitest run` as the PRIMARY gate. Even if the operator skips the on-device run entirely, the automated tests still catch wiring regressions. D-07 design. |
| T-06-15 | Repudiation | unsigned-off Phase 6 ships without device verification | accept | The "Sign-off" block (operator + date + commit) provides an opt-in audit trail. Not enforced — D-07 secondary gate is by design. |
</threat_model>

<verification>
After the task completes, run from the project root:

1. `test -f .planning/phases/06-alarmsession-refactor-v1-regression-guard/06-REGRESSION-CHECKLIST.md` — must exit 0.
2. `wc -l .planning/phases/06-alarmsession-refactor-v1-regression-guard/06-REGRESSION-CHECKLIST.md` — line count must be >= 60.
3. `grep -c '\- \[ \]' .planning/phases/06-alarmsession-refactor-v1-regression-guard/06-REGRESSION-CHECKLIST.md` — must report at least 20.
4. `git diff --name-only HEAD -- src/` — must produce zero output.
5. `git diff --name-only HEAD -- .planning/phases/06-alarmsession-refactor-v1-regression-guard/` — must include `06-REGRESSION-CHECKLIST.md` as a new file.
</verification>

<success_criteria>
- `06-REGRESSION-CHECKLIST.md` exists at the phase directory.
- File covers five sections matching CONTEXT `<specifics>` lines 189–195.
- File frames automated tests as the primary gate and itself as the secondary gate (D-07).
- File explicitly cross-references SEG-05 and LAND-05.
- No source file is modified by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/06-alarmsession-refactor-v1-regression-guard/06-03-SUMMARY.md` capturing:
- Confirmation that the checklist file exists with the required five sections.
- The total line count of the checklist file.
- The total checkbox count.
- Confirmation that no source file was modified.
</output>
